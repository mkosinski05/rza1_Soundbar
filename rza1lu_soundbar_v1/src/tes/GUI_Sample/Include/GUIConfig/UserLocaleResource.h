#if !defined(USER_LOCALE_RESOURCE_H)
#define USER_LOCALE_RESOURCE_H

/**
User-defined Locale-handles.
Each of these IDs maps to a localization-object within the GUI.
Each entry has to look like:
ENTRY(LT_USERLOCALE01)    \*/
#define USER_LOCALE_TABLE


#endif // USER_LOCALE_RESOURCE_H
